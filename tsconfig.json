{
    // 어디에 타입스크립트 파일이 위치하는지 알려줘야한다. -> include에 자바스크립트로 컴파일하고 싶은 디렉토리들을 넣어준다.
    "include": [
        "src"
, "src/.myPackage.d.ts"    ],
    "compilerOptions": {
        // 컴파일된 자바스크립트 파일이 생성될 디렉토리
        "outDir": "build",

        // 어떤 버전의 자바스크립트로 컴파일할 지 정할 수 있다.
        "target": "ES6",

        // 합쳐진 라이브러리의 '정의 파일'(declaration files)을 특정해주는 역할을 한다.
        // 정의 파일이 목표로 하는 런타임 환경을 알려준다.(컴파일된 자바스크립트 코드가 어디에서 동작할지 알려주는 것)
        "lib": ["ES6", "DOM"],
        // DOM을 세팅해두면 타입스크립트는 document를 고려할 수 있게된다.(브라우저의 API와 타입들을 알고 있게 된다.)

        "strict": true,

        "allowJs": true,
    }
}

// 정의 파일 (declaration)
// 타입스크립트는 내장된 자바스크립트 API를 위한 기본적인 타입 정의를 가지고 있다.
// 타입 정의: 타입스크립트에게 자바스크립트 코드의 타입을 설명해주기 위해 사용
// 많은 경우 다른 패키지, 프레임워크, 라이브러리를 사용하는데 대부분의 경우 자바스크립트로 코드가 작성돼있다.
// 그런 경우 타입 정의가 돼있지 않으면 그 많은 코드의 타입들을 알 수가 없다. -> 타입 정의 작성 방법을 알아야한다.
// 자바스크립트 파일의 모양(타입)을 설명해주는 것
